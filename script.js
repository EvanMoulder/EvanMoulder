// Find the button by its ID in the HTML
const button = document.getElementById("coolButton");

// Add a "click" event listener so something happens when the button is clicked
button.addEventListener("click", () => {
  // Show a popup message when the button is clicked
  alert("You clicked the magic button! 🌟");
});
